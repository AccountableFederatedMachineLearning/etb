
a(3, [4, 3]).

e(X, Z) :- a(X, [Z, X]).

e(X) :- length('[1,2,4,3]', X).

f(12) :- length('[1,2]', 2).

e(X, 7) :- length(X, 4, 3).

b([Z,X], [Y, Z],Z) :- a(Y, [Z, X]).
/* b(X, (Y, Z)) :- a(Y, (Z, X)). */
c(X, [Y, 9]) :- a(Y, [X, X]).
/* d(X, (Y, Z)) :- a(Y, (Z, X)). */

len(nil, 0).
len([X, Y], 1) :- len(Y, 0), ok(X).

a(3, 4, 5).

b(X, Y, Z) :- a(Y, Z, X).
